# Supervisor verification notes

## Direct implementation approved

The user approved main-agent direct takeover after the stopped Luna worker. Routing is no longer blocked. All partial changes remain owned by this task; no merge, push, deploy or production writes are authorized. Original seven-task scope remains in force.

### 2026-09-09 continuation evidence

- Commit `e462435` adds real posted-parent `INSERT`/move protections to migration 0065 and a disposable-PostgreSQL regression; commit `a2d6df9` binds cancellation to reason, revision, idempotency, and an append-only receipt; commit `712326f` wires the upload-first batch editor and enables default discovery of `frontend/src` tests.
- Current focused staging/migration verification is 13 passed, 0 failed, 64 expectations; frontend verification is 62 files/274 tests passed, lint/build passed; plugin verification is 56 passed and validator passed. These do not close the full plan.
- Full backend disposable suite result: 900 passed, 3 skipped, 6 failed across 909 tests / 131 files. Remaining failures are two DB-cleanup deadlocks, one renewal concurrency race (`RENEWAL_NOT_REVERSIBLE`), and three floating penalty expectation mismatches fixed by the focused run below. This is not final acceptance.
- Correction 1 diagnosis: the prior 40P01 cycles were cross-file Bun worker/database-reset interference; `--max-concurrency=1` did not serialize files. The disposable runner now uses `--parallel=1`. The two named service files passed together 36/36 and 143 expectations under the corrected runner. A subsequent full rerun was blocked by Docker/PostgreSQL storage exhaustion (`No space left on device`) after 850 pass / 3 skip / 56 cascading DB-write failures; no financial assertion was relaxed. Evidence is preserved under `/tmp/creditsync-batch-resume-correction1-rerun2/`.
- Cleanup continuation: anonymous-volume growth was addressed without global pruning. Each run now records a generated labeled volume and removes only that volume and its exact container, including the EXIT failure path. Migration smoke passed 4/4; an invalid-filter failure-path run also left no owned volume/container. Full-suite rerun is next at this corrected HEAD.
- User-resume full suite reached 905 pass / 3 skip / 1 fail. The remaining failure was a real precedence gap: principal reprojection could throw before the shared later-floating-payment guard. The guard is now applied before principal reprojection for principal allocations; focused payment-service verification is 37/37 and full-suite rerun is required at the new HEAD.

Direct TDD checkpoints: staging initial 0/5 red -> 7/7 green, including immutable receipts/evidence; discovered and repaired the legacy batch trigger's erroneous UPDATE RETURN OLD in additive migration 0065. Atomic shuffled-order, stale timestamp and incomplete coverage initially failed (3 failures), then passed. Concurrent preview version test reproduced a unique-key race and passes after borrower-first/batch locking. Single-post bypass and older proposal-only intake both reproduced then passed after shared pending dependency checks. Latest focused staging/service/atomic suite before component work: 21 tests passed; backend typecheck passed. These are partial checkpoints, not full acceptance.

## Direct takeover: restore and secondary borrower checkpoint

- Reproduced false stale floating restore. Cause: JSON object insertion-order hashing disagreed with persisted PostgreSQL jsonb provenance. Hashing now sorts object keys recursively, preserving array order; no weakening of stale-state validation.
- Restore provenance is rebuilt per original transaction rather than merged per loan; original 40.00 + 60.00 interest restores as two transactions, consumes each plan once, binds concrete accrual identities and checks aggregate remaining accrual capacity. Execute rebuilds source provenance after locks instead of trusting stored preview plans.
- Restore previews bind finalized child evidence UUID/checksum/MIME/size/finalization snapshots. A changed child evidence set rejects the confirmed preview. First success and retry return the same public result/correlation metadata in the regression fixture.
- Independent Rawls review found principal over-capacity clamping, missing floating penalty attribution, and inactive-target preview/execute disagreement. Reproduced all three with real disposable DB tests. Principal capacity is checked cumulatively at preview and execute; preview uses the same target resolver. Floating fee/penalty restore explicitly fails closed pending complete component provenance replay. This is a safety gate, NOT completed penalty restore or temporal reflow.
- Reproduced a single-payment bypass of an older secondary borrower item in a multi-borrower batch. Shared chronology queries now inspect latest per-item batch allocation mappings, and borrower lock discovery includes them. Regression also verifies whole-batch rollback preserves exact pre-execution intake statuses.
- Latest focused verification: `./scripts/test-disposable-postgres.sh src/services/payment-restore-floating.integration.test.ts src/services/payment-reconciliation-service.test.ts src/services/payment-service.test.ts src/services/payment-batch-atomic.integration.test.ts`: 70 pass, 0 fail, 306 expectations, no skips (`/tmp/creditsync-restore-chronology-checkpoint.log`). Backend `bun run typecheck` passed. These are worktree checkpoint results, not final implementation HEAD acceptance.
- Rawls scoped re-review accepted all three safety fixes. Follow-up restore coverage then verifies principal totals across two original transactions (40.00 + 160.00 versus capacity 180.00) and execute-time capacity rejection leaving the child draft with no transactions. Restore integration rerun: 6 pass, 0 fail, 18 expectations (`/tmp/creditsync-restore-capacity-final-checkpoint.log`). Fee-only provenance coverage and complete temporal replay remain open; no final whole-branch review claimed.
- Full plan remains incomplete: temporal reflow, full restore component replay, staging edits/split/dependencies, additional chronology decisions, complete REST/MCP/plugin contracts, real four-step Web/OCR workflow, final verification/browser QA/commit gates. No commit/merge/push/deploy; no live financial data accessed or changed.
- Continuation after the superseded pause: removed an overly-early floating chronology assertion so component-specific penalty compensation failures remain observable; the later transaction/allocation guard still blocks unsafe backdated writes. Focused floating regression is green (21/0, 122 expectations). Fixed the same-call staged-item UUID mapping in the upload-first UI; targeted test, lint, build, and backend typecheck are green. Full suite remains non-green; exact failures are recorded above.

## Earlier routing pause (superseded)

### Direct takeover continued checkpoints

- Shared scheduled waterfall extracted as `planScheduledPayment`; preview projects paid/remaining schedule state, execution reuses the same function. Components 20 principal + 7 interest + 3 fee no longer appear as all-principal.
- Shared floating waterfall and provenance target selection extracted from `postPayment`. Read-only projection accounts for prior batch principal, interest, penalty and carried balances. Synthetic 75+45 on 07/08 and principal reduction 1000 causing next-day interest 60 both preview/execute green.
- Migration 0066 adds explicit scheduled/floating target constraints, stored allocation snapshot hash and posting sequence. Execute compares latest accounting/membership/evidence hash, revision and allocation/sequence snapshot, then verifies posted components before transaction commit. Legacy previews without snapshot are rejected.
- Missing-day decisions have append-only rows, audited idempotent receipts, reviewed reason/range, and apply to one revision. Decision invalidates old preview; next preview may clear only covered calendar-gap warnings. This does NOT yet implement all required backdated/advance/tie/split decisions or public UI/MCP wiring.
- Capture concurrent retries now serialize and return canonical client-key order, exact persisted result, reject payload/portfolio replay mismatches. Data-only capture still follows optional evidence semantics; upload-first review creates evidence-required intakes.
- Independent read-only Erdos review reported three issues. Main reproduced and fixed future penalty compensation projection and advance-interest/principal paid-accrual conflict. Constraint test initially hit a Drizzle thenable test API mistake (not a valid feature RED); corrected real DB test verifies `CHECK (... IS TRUE)` rejects incomplete floating JSON.
- Focused four-file staging/service/atomic/floating suite: 30 pass, 0 fail, 101 expects (`/tmp/creditsync-batch-reviewed-green.log`), plus backend typecheck passed. Later multi-borrower atomic regression: 12 pass, 0 fail (`/tmp/creditsync-multi-borrower-green2.log`); implementation supports explicit per-allocation borrower mappings and global rollback.
- Full backend intermediate checkpoint: 885 pass, 3 skip, 2 fail. MCP receipt shape mismatch repaired preserving frozen output; smoke-only unresolved drafts no longer falsely declare themselves older dependencies of another test workflow. Payment test now separates intentional older unresolved drafts from independent double-post race. Focused MCP rerun: 16 pass, 0 fail (`/tmp/creditsync-adapter-green.log`). Full final suite still REQUIRED.

Remaining major work: all-writer locking, richer chronology blockers and decisions, split/dependency, staging review revisions and OCR/workspace APIs, target/candidate access and robust input validation, reconciliation/reflow and restore review findings, four-step UI, public REST/MCP/plugin contracts, final suites/browser QA/independent final review. No implementation commit, merge, push or deploy has occurred. Main checkout untouched.

The Luna/medium worker was stopped after two corrective checkpoints. Partial implementation is NOT accepted and MUST NOT be merged/deployed. It still has the independent P1 findings in review.md; Task1 changes have not completed their regression gate. Worker repeatedly implemented service/schema before reproducing requested failures, despite explicit TDD feedback. No implementation commit was made. Preserve all partial code for review, do not erase it or claim completion. Request user approval before changing the approved Luna/tmux implementation routing. Main checkout remains fde6b51 clean. Verified results below are limited baseline/helper checks, not full feature acceptance.

## Baseline fde6b51

Independently reran `./scripts/test-disposable-postgres.sh src/mcp/default.test.ts` in the feature worktree backend after full-suite MCP smoke timeout. Exit 0: 16 pass, 0 fail, 566 expectations; frozen-tool smoke took 3913.60 ms, whole file 8.45 s. Local log: /tmp/creditsync-batch-baseline-mcp.log. This is baseline evidence only, not final feature verification. Do not change assertions merely to hide the full-suite timeout; rerun at final HEAD.

Review emphasis: all writers must acquire borrower lock before loan/intake locks consistently; otherwise cross-path deadlocks survive a shared helper. Existing restore-mode currentProvenancePlans remains empty while execute requires floating plan; verify preview and execute parity with real disposable DB tests. No production access has been made in this implementation run.

## Early diff review (not final findings)

Progress accuracy: execution log currently checks Tasks 2/3/5 while their own descriptions list unimplemented acceptance; uncheck until complete. Task 5 includes temporal compensating reflow, not just restore parity. A static-source test or fixture constraint failure is not a demonstrated feature RED; record separately. Pending warning acknowledgement, all-writer borrower guard, immutable receipts, projection/planner parity and multi-borrower support remain required, not optional later gates.

Staging evidence review: prepare must NEVER issue PUT URL for already-ready evidence (would allow overwriting a finalized/posted slip). Finalize must check upload expiry and actor/batch access, not just tenant; enforce MIME allowlist and max size. Prepare/finalize need audit and operation fingerprint/receipts. Review replay currently returns internal numeric paymentIntakeId/batchItemId and ignores changed payload/key: return original public UUID result and reject changed request. Review must lock batch/staging, reject posted/cancelled batch, increment batch revision and invalidate confirmation, and establish readonly evidence storage provenance. Test each with real DB. Current schema operation_key per item is not an append-only multi-operation receipt log; fix rather than claim that requirement done.

SUPERVISOR CHECKPOINT: current planFloatingBatch is NOT accepted. It invents a separate component waterfall and merely drains supplied balances without authoritative per-date accruals, so next-day interest can become principal. It is not wired into batch preview (floating loans still excluded). Replace this newly-created implementation using a genuinely shared/extracted accounting planner, with real DB 07/08 multi-contract integration assertions. A staging row created immediately alongside an intake is NOT the approved upload-before-amount/time workflow. Add public service/API for unreviewed staging evidence and review transition; retain batch unposted. Source-string migration assertions do not establish constraints: run real DB insert/update tests. Do not mark tasks completed until their user-visible/service behavior works end-to-end. Read this supervisor note at every checkpoint, and update execution log with actual progress, not initial dependency failure only.

The initial chronology kernel sorts timestamp strings lexically; add an offset-equivalence regression and compare epoch instants instead. Invalid receivedAt currently reaches the calendar-gap formatter despite earlier blocker; skip invalid timestamps in date-gap calculation. Advance obligation comparison must use Bangkok date, not receivedAt.slice(0,10). Guard status ready with warnings is only safe if EVERY consumer requires acknowledged preview-bound decisions. Filter terminal pending statuses at runtime rather than asserting them to a narrower TS union. Current first batch adapter inspects only other batches and one borrower; completion still needs resolved standalone drafts, all borrowers, execute-time locks, and single-post enforcement.
