# CreditSync batch chronology safety — execution log

Plan: `docs/superpowers/plans/2026-09-09-batch-chronology-safety.md`
Branch: `codex/batch-chronology-safety`
Base: `fde6b51`
Scope: implement and verify all seven approved tasks; no merge, push, deploy, production access, financial writes, or paid OCR.

## Progress

### 2026-09-10 — Remote continuation verification and provenance hardening

- Focused disposable reflow repair and migration coverage passed after installing locked Bun dependencies in this worktree; backend typecheck, frontend lint/build, and plugin 10.0.0/129-tool validation passed. The repair service now ignores non-finalized evidence and checks source entry/component conservation before planning.
- A serialized full backend rerun was started alone with a runner-owned disposable PostgreSQL instance. It reproduced existing 5-second timeout/deadlock cascade failures in unrelated intermediated-disbursement and floating-penalty suites; the evidence is retained at `.codex-task-logs/backend-full-final.log` and is not treated as green.
- Detailed acceptance evidence and remaining gaps are recorded in `docs/superpowers/plans/2026-09-10-batch-chronology-evidence-matrix.md`.

### 2026-09-10 — Task 5 existing-data temporal-reflow repair checkpoint

- Implemented `backend/src/services/payment-reconciliation-reflow-service.ts` using the existing authoritative floating allocator and append-only reflow tables. Preview locks tenant-authorized borrowers, derives the Bangkok effective date, validates legacy replacement/source transaction lineage and interest-only components, snapshots safe transaction/evidence metadata, and writes no financial records. Execute re-reads the proposal/context under locks, rejects stale/expired/unsupported/already-repaired state, appends signed reversal/replacement provenance atomically, rebuilds touched accrual projections, and returns the durable exact replay result.
- Added public MCP tools `payment.reconcile.reflow.preview` and `payment.reconcile.reflow.execute`; synchronized backend closed schemas, direct application handlers, plugin 10.0.0/129-tool contract, reconciliation skill, and two eval scenarios. No REST endpoint or duplicate reverse/restore ledger was added.
- Focused RED `/tmp/creditsync-batch-resume.cs2Qp2/task9-reflow-bangkok-red.log` caught UTC slicing at a Bangkok evening boundary; GREEN `/tmp/creditsync-batch-resume.cs2Qp2/task9-reflow-bangkok-green.log` passes `2 tests, 0 failures, 11 expectations`. The suite now also covers preview no-write, exact replay/key conflict, already-repaired rejection, expiry/tenant denial, and injected mid-provenance-write rollback. Plugin/MCP focused checks and backend typecheck are still pending at this checkpoint.
- Remaining Task 5 evidence includes broader multi-loan legacy fixtures and full reflowed reverse/restore inspection; authenticated browser QA, final full gates, and other approved plan tasks remain open.

### 2026-09-10 — Task 7 OCR runtime and remaining acceptance audit

- Application OCR runtime smoke `/tmp/creditsync-batch-resume.cs2Qp2/task8-ocr-runtime-smoke.log` invoked `backend/src/lib/ocr.ts` through installed `tesseract.js@7.0.0` against a synthetic dependency PNG: `ok`, 23,940 input bytes, 2 output characters. This is a synthetic English/runtime smoke only, not bank-slip accuracy evidence. The system `tesseract` CLI is irrelevant and was not installed.
- RED `/tmp/creditsync-batch-resume.cs2Qp2/task8-ocr-cleanup-red.log` reproduced recognition failure without the injected worker lifecycle seam; GREEN `/tmp/creditsync-batch-resume.cs2Qp2/task8-ocr-cleanup-green.log` passes the failure-cleanup assertion. `extractTextFromImage` now terminates a created Tesseract worker in `finally`; raw OCR remains transient. Real disposable OCR focused gate `/tmp/creditsync-batch-resume.cs2Qp2/task8-ocr-focused-final.log` passed `8/8, 29 expectations`.
- Task 5 audit found the existing scheduled legacy repair service at `backend/src/services/payment-allocation-correction-service.ts` with its existing MCP contract, but no implementation exists for the approved temporal-reflow existing-data repair preview/execute described in `docs/superpowers/plans/2026-09-07-floating-reconciliation-temporal-reflow.md` (the required `payment-reconciliation-reflow-service.ts`, dedicated repair preview/execute path, and repair MCP tools are absent). This remains an explicit unimplemented acceptance gap; no speculative financial repair path or live repair was added.
- Browser inspection found only unit/component Vitest coverage and production auth middleware using `JWT_SECRET`/Google-facing application flow; no existing safe authenticated browser fixture/harness was available to exercise a real backend tenant without inventing an auth bypass. Browser E2E remains unverified and is not represented as green.

### 2026-09-10 — Task 7 final post-OCR-fix gates

- Fresh full serialized backend at `3f42d75` passed in `/tmp/creditsync-batch-resume.cs2Qp2/task8-final-full-backend.log`: `970 pass, 3 skip, 0 fail, 6181 expectations`, `973 tests across 136 files`, 187.71s. The added OCR worker-lifecycle test is included; no financial invariant was skipped.
- Fresh OCR evidence: application `extractTextFromImage` runtime smoke `/tmp/creditsync-batch-resume.cs2Qp2/task8-ocr-runtime-smoke.log` succeeded through Tesseract.js/WASM on a synthetic PNG; cleanup RED/GREEN are `/tmp/creditsync-batch-resume.cs2Qp2/task8-ocr-cleanup-red.log` and `/tmp/creditsync-batch-resume.cs2Qp2/task8-ocr-cleanup-green.log`; focused disposable OCR is `/tmp/creditsync-batch-resume.cs2Qp2/task8-ocr-focused-final.log` (`8 pass, 0 fail, 29 expectations`). This verifies runtime/lifecycle only, not bank-slip OCR quality.
- Backend typecheck `/tmp/creditsync-batch-resume.cs2Qp2/task8-final-typecheck.log`, frontend test/lint/build `/tmp/creditsync-batch-resume.cs2Qp2/task8-final-frontend-test.log`, `/tmp/creditsync-batch-resume.cs2Qp2/task8-final-frontend-lint.log`, `/tmp/creditsync-batch-resume.cs2Qp2/task8-final-frontend-build.log`, and plugin test/validator `/tmp/creditsync-batch-resume.cs2Qp2/task8-final-plugin-test.log`, `/tmp/creditsync-batch-resume.cs2Qp2/task8-final-plugin-validate.log` all exited 0. Frontend remains `62 files, 293 tests`; build has the existing chunk-size warning; plugin remains `9.6.0 / 11 skills / 127 tools`.
- The three full-suite skips remain cache-only tests gated by absent `CACHE_URL`; they do not cover financial chronology. Task 5's temporal-reflow existing-data repair preview/execute remains unimplemented in the current code (`payment-reconciliation-reflow-service.ts` and repair MCP tools are absent), while scheduled allocation correction is implemented separately. Authenticated browser E2E and production-quality OCR remain unverified; full seven-task completion is not claimed.

### 2026-09-10 — Task 7 full-gate diagnosis checkpoint

- Full backend evidence `/tmp/creditsync-batch-resume.cs2Qp2/task6-fix-ocr-final-full-backend.log` was not green at `966 pass, 3 skip, 3 fail`; one OCR assertion was corrected afterward and is green in `/tmp/creditsync-batch-resume.cs2Qp2/task6-fix-ocr-final-disposable.log`. The two remaining failures were investigated rather than labeled preexisting: MCP reconciliation preview traced to reversal allocation `-15.00` entering temporal-reflow loading and is fixed in this commit; restore first-test timeout remains a full-suite-only performance issue pending a fresh full run, while the isolated file passed `8/8` in `/tmp/creditsync-batch-resume.cs2Qp2/task6-fix-ocr-restore-repro.log`.
- RED/root-cause trace `/tmp/creditsync-batch-resume.cs2Qp2/task7-mcp-debug.log` captured only non-sensitive error type/message and identified the negative signed allocation. Targeted MCP after the fix `/tmp/creditsync-batch-resume.cs2Qp2/task7-mcp-final-targeted.log` passed `18/18, 636 expectations`; no raw evidence or financial contents were logged.
- The first full run's DDL timeout was reproduced as a schema-reset cascade: the migration test timed out while its destructive reset was contended, then later tests observed `no schema has been selected`/missing relations. The same migration file passed independently in `/tmp/creditsync-batch-resume.cs2Qp2/task7-migration-isolated.log` (`8 pass, 0 fail, 26 expectations`); no timeout was increased and no assertion was skipped.

### 2026-09-10 — Task 7 final verification at 3f681146

- Full serialized backend command `cd backend && ./scripts/test-disposable-postgres.sh` passed in `/tmp/creditsync-batch-resume.cs2Qp2/task7-final-full-backend-rerun.log`: `969 pass, 3 skip, 0 fail, 6179 expectations`, `972 tests across 135 files`, 186.62s. The signed-reversal reflow regression and prior OCR assertion are green in this run.
- The three skips are cache integrations gated by absent `CACHE_URL`: `loan restructure REST contract > invalidates a prewarmed loan-detail cache after restructure execution`, `loan list borrower labels > refreshes cached loan-list rows after alias confirmation and deactivation`, and `borrower service > invalidates tenant caches after borrower updates`. They cover cache invalidation only; they do not bypass payment, ledger, posted-history, chronology, migration, or reflow invariants.
- `cd backend && bun run typecheck` passed in `/tmp/creditsync-batch-resume.cs2Qp2/task7-final-typecheck.log`; `cd frontend && bun run test`, `bun run lint`, and `bun run build` passed in `/tmp/creditsync-batch-resume.cs2Qp2/task7-final-frontend-test.log`, `/tmp/creditsync-batch-resume.cs2Qp2/task7-final-frontend-lint.log`, and `/tmp/creditsync-batch-resume.cs2Qp2/task7-final-frontend-build.log` (`62 files, 293 tests`). Build retained the existing >500 kB chunk warning.
- `cd plugins/creditsync && bun test` passed `56/56, 1710 expectations` in `/tmp/creditsync-batch-resume.cs2Qp2/task7-final-plugin-test.log`; `bun run validate` passed in `/tmp/creditsync-batch-resume.cs2Qp2/task7-final-plugin-validate.log` (`9.6.0, 11 skills, 127 tools`).
- Full-run OCR evidence is indexed at `/tmp/creditsync-batch-resume.cs2Qp2/task7-ocr-full-evidence.log`; local `tesseract` was not installed (`/tmp/creditsync-batch-resume.cs2Qp2/task7-tesseract-search.log`). No safe authenticated browser harness was found without bypassing production Google auth; browser evidence remains open. Task 5 legacy repair/lifecycle coverage remains explicitly incomplete in the earlier ledger entries.

### 2026-09-10 — Task 6 OCR safety hardening checkpoint

- TDD RED `.codex-task-logs/task6-fix-ocr-red-parser.log` reproduced the Bangkok 00:30/year-boundary rejection and numeric receiver leakage. GREEN `.codex-task-logs/task6-fix-ocr-disposable2.log` passed the real disposable OCR service suite `7 pass, 0 fail, 26 expectations`, including Bangkok local-calendar/leap/CE-vs-BE validation, allowlisted names, finalized-byte SHA-256 verification, no financial writes, and concurrent same-key extraction returning one exact receipt/audit.
- Extraction now serializes the tenant/key command with a transaction advisory lock, locks batch then staging and rereads authorization/evidence state, verifies downloaded bytes before invoking OCR, and rechecks revision/evidence after transient extraction. The UI binds delayed OCR results to generation, staging revision, and ready evidence; stale responses cannot overwrite current review state.
- Final focused disposable `.codex-task-logs/task6-fix-ocr-final-disposable.log` passed `7/7, 27 expectations`; final frontend `.codex-task-logs/task6-fix-ocr-final-frontend2.log` passed `62 files, 293 tests`, lint/build passed with the existing chunk-size warning, and backend typecheck `.codex-task-logs/task6-fix-ocr-final-backend-typecheck2.log` passed. Full backend `.codex-task-logs/task6-fix-ocr-final-full-backend.log` reached `966 pass, 3 skip, 3 fail`; after correcting one test-only code-vs-message assertion, the remaining failures reproduce independently in existing `payment.reconcile.preview` MCP adapter setup (`17 pass, 1 fail`) and full-suite restore 5-second contention (restore file alone `8 pass, 0 fail` in 1.71s). Plugin remains `56 pass, 1710 expectations`, validator `9.6.0/127 tools`. Authenticated browser QA was not run without a safe synthetic auth harness; full backend green and browser verification remain open.

### 2026-09-10 — Task 6 local OCR staging boundary checkpoint

- Added `extractPaymentBatchStagingItem` and closed REST/MCP `payment.batch.staging.extract`. It requires finalized staging evidence, performs local Tesseract extraction only, returns normalized candidate amount/time/payer/receiver/fee plus hashed reference and evidence checksum, and stores an append-only staging operation receipt bound to the current staging revision/evidence. Raw OCR text is transient and never returned, persisted, or logged; extraction/retry creates no intake, ledger, allocation, or transaction.
- TDD evidence: `.codex-task-logs/task6-ocr-red-command.log` reproduced the missing service export, `.codex-task-logs/task6-ocr-green-parser3.log` passed parser RED/GREEN cases, and `.codex-task-logs/task6-ocr-disposable2.log` passed the real disposable staging/evidence/extraction/retry suite `28/28, 111 expectations` across the new extraction cases and existing staging regressions. The initial disposable invocation used an incorrect post-`cd backend` path and found no tests; it was corrected and rerun, not counted as behavioral evidence.
- Frontend now exposes an explicit candidate-only extraction/apply-to-review step after evidence is ready, with paired TH/EN copy and persisted stable draft metadata. Focused editor/model tests passed `22/22`, including the component OCR boundary; final full frontend passed `62 files, 292 tests`, lint and build passed (existing chunk-size warning). Backend typecheck passed. MCP/server passed `37 pass, 14 existing DB skips`; plugin passed `56/56, 1710 expectations`, validator passed at plugin `9.6.0`, `11 skills`, `127 tools`; frozen contract was regenerated from the local MCP tools/list exchange.
- Scope limits: this verifies synthetic/injected extraction and the local Tesseract boundary, not OCR accuracy on real slips; no paid provider, real image, live MCP, or production auth was used. Authenticated browser E2E, broader OCR quality fixtures, temporal reflow/full financial verification, and remaining full-plan gates stay open.

### 2026-09-10 — Task 5 evidence and lifecycle verification checkpoint

- RED `.codex-task-logs/task5-evidence-red.log` reproduced a ready-evidence replacement being accepted after reconciliation preview. The shared reconciliation source snapshot now records only safe finalized evidence identity/checksum/type/size/time metadata and the execute hash recheck rejects changed evidence before writes; GREEN `.codex-task-logs/task5-evidence-green2.log` passes the public prepare/finalize/preview/execute scenario with zero transaction/allocation/proposal/execute-audit writes.
- Serialized reconciliation, restore, and reflow kernel coverage passed `41/41, 200 expectations` in `.codex-task-logs/task5-round4-focused-final.log`. It includes restore-child evidence replacement parity, temporal-reflow repeat-compensation rejection, multi-transaction replay, rollback, two-loan barriered replay, and fail-closed unsupported provenance. Backend typecheck passed in `.codex-task-logs/task5-round4-typecheck-final.log`.
- The existing strict MCP preview/execute contract remains synchronized at plugin 9.5.0/126 tools and was not changed by this evidence-only field addition because the service already exposes `sourcePayment` as the safe transport envelope with the strict temporal-reflow plan field from the prior checkpoint. An explicit reflow-specific reverse/restore endpoint is not present; current supported restore uses the original source plus a separate finalized child evidence set, and repeated compensation is blocked. OCR/browser/full-plan gates remain open.

### 2026-09-10 — Task 5 financial integration verification checkpoint

- Added an actual application-execute rollback fixture using a disposable PostgreSQL trigger that fails only after the first signed reflow entry; `payment-reconciliation-service.test.ts` passed `21/21, 148 expectations` in `.codex-task-logs/task5-round3-service-final.log`. The test removes the owned trigger/function in `finally` and proves transactions, allocations, reflow groups/entries, accrual caches, loans, intake/proposal state, and audits are unchanged. The final serialized combined reflow/kernel/service/migration gate passed `36/36, 191 expectations` in `.codex-task-logs/task5-round3-focused-final2.log`.
- Added a two-floating-loan fixture with multiple later payments and explicit `75.00 + 45.00` backdated allocation. A borrower lock barrier proves concurrent identical execute requests serialize and return the same receipt/correlation, while a changed-payload retry conflicts without another effect; preview/execute due-date projections, per-loan conservation, signed compensation/replacement pairing, original transaction snapshots, and all-or-nothing lineage are asserted.
- Supervisor parity evidence at `/tmp/creditsync-batch-resume.cs2Qp2/supervisor-reflow-parity.log` reported `34 pass, 0 fail, 156 expectations` before this checkpoint. MCP/plugin contract gates remain unchanged from the prior synchronized 9.5.0/126-tool checkpoint. Remaining stale-preview-after-accounting/evidence mutation breadth and inspect/reverse/restore follow-up coverage remain explicit gaps; OCR/browser/full-plan gates remain open.

### 2026-09-10 — Task 5 sequential temporal-reflow integration checkpoint

- `buildTemporalReflowPlanForLoan` now carries each prior authoritative replacement projection into the next chronological resolver call; source loading scans active ledger components before rejecting unsupported or incomplete provenance. The serialized disposable reflow integration passed `30/30, 131 expectations` in `.codex-task-logs/task5-round2-db-disposable.log`; non-DB smoke passed `17/28` with 13 database tests skipped in `.codex-task-logs/task5-round2-db-final.log`.
- Preview and execute compare the same two-transaction plan, preserve original transactions/allocations, append signed compensation/replacement lineage, rebuild touched accrual totals, and retain exact-key replay coverage. MCP preview now exposes a strict temporal-reflow impact object and execute exposes `reflowGroupPublicId`; the frozen plugin contract is synchronized to 9.5.0 and 126 tools.
- Remaining mandated failure-injection rollback, two-affected-loan conservation, stale confirmation, and full OCR/browser/plan gates require further verification; no full-plan completion is claimed.

- [x] Baseline verification and repository mapping
- [ ] Task 1: durable batch staging and retry (service/API, resumable receipts/evidence checks, posted-membership DB gates, read-only workspace, revision-bound edits, atomic split/dependency APIs, and round-three mapping/lock fixes are green; broader chronology/review acceptance remains)
- [ ] Task 2: shared chronology guard (partial; all-writer locking and decision persistence remain)
- [ ] Task 3: floating multi-contract planner (partial; projected sequential state and accounting parity remain)
- [ ] Task 4: bound atomic execute (existing rollback/concurrency green; preview binding and all-borrower chronology recheck remain)
- [ ] Task 5: reconciliation and restore (unaccepted worker changes; provenance and reflow review findings remain)
- [ ] Task 6: REST, MCP, UI and contract synchronization (upload-first four-step UI and default test discovery now green; candidate discovery, split/decision/cancel public contract, and frozen MCP sync remain)
- [ ] Task 7: full verification and handoff

## Verification log

### 2026-09-09 — baseline

- `bash backend/scripts/test-disposable-postgres.sh` — blocked before tests: `drizzle-kit: command not found`.
- `cd backend && bun run typecheck` — blocked: `tsc: command not found`.
- `cd frontend && bun run test` — blocked: `vitest: command not found`.
- `cd frontend && bun run lint` — blocked: `eslint: command not found`.
- `cd frontend && bun run build` — blocked: `tsc: command not found`.
- `cd plugins/creditsync && bun test && bun run validate` — partial: 7 documentation tests passed; dependency/module resolution failed for `ajv` and backend MCP SDK imports.

Dependencies were absent from the checkout; installation and baseline rerun are pending.

Dependencies were then installed with `bun install` in `backend/`, `frontend/`, and `plugins/creditsync/` (no production access). Baseline rerun results:

- `cd frontend && bun run test` — 252 passed, 0 failed.
- `cd frontend && bun run lint` — passed.
- `cd frontend && bun run build` — passed; existing chunk-size warning only.
- `cd backend && bun run typecheck` — passed.
- `cd plugins/creditsync && bun test && bun run validate` — 56 tests passed; validator passed (`9.2.0`, 11 skills, 116 tools).
- `cd backend && bash scripts/test-disposable-postgres.sh` — 857 passed, 3 skipped, 1 failed; baseline full-suite MCP adapter test timed out at 10s. Independent focused rerun `./scripts/test-disposable-postgres.sh src/mcp/default.test.ts` — 16 passed, 0 failed, 566 expectations. This is baseline evidence, not final acceptance.

### 2026-09-09 — implementation checkpoints

- TDD chronology: `bun test src/services/payment-chronology-guard.test.ts` first went red because epoch ordering changed blocker order; expectation was corrected, then 8 passed / 0 failed. `bun run typecheck` passed. Coverage now includes offset-equivalent instants, invalid timestamp gap skip, Bangkok date comparison, and runtime terminal filtering.
- TDD staging integration: `./scripts/test-disposable-postgres.sh src/services/payment-batch-service.test.ts` passed 7 / 0 (20 expectations). It verifies staging with null amount/time and null intake/item, fake signed prepare/finalize, review-created intake, and evidence provenance. It also verifies shared floating 07/08 preview components at 75.00 interest each with zero transaction writes.
- The first floating DB run was red on `loans_interest_period_policy_completeness_check`; the fixture was corrected with the required daily policy fields and the same disposable integration command passed.
- Existing atomic execute integration gates passed in that run: injected second-stage failure left intake/transactions/schedule unchanged, and concurrent same-key execution produced one repayment.
- REST additions: upload-first staging/review routes under `/payment-batches/stage` and `/payment-batches/staging/:id/...`; restore draft/evidence/preview/execute/backfill routes under `/payment-restores`.
- Restore execute now binds the preview's source floating provenance in restore mode and uses the same floating ledger balance version; an end-to-end restore integration test is still required.
- `./scripts/test-disposable-postgres.sh src/db/atomic-batch-payment-migration.test.ts` — 3 passed, 0 failed, 21 expectations; real DB staging nullability, tenant FK, and unique client key checks passed. The migration source-text checks remain only supplemental.
- `cd frontend && bun run test` — 252 passed, 0 failed across 53 files; existing navigation warning emitted.
- `cd frontend && bun run lint` — completed successfully (no diagnostics returned).
- `cd frontend && bun run build` — passed; existing >500 kB chunk warning only.
- `cd plugins/creditsync && bun test && bun run validate` — 56 passed, 0 failed; validator passed (`9.2.0`, 11 skills, 116 tools).
- `cd backend && bun run typecheck` — passed after staging, nullable floating schedule, borrower-lock, chronology, and restore changes.

- `./scripts/test-disposable-postgres.sh src/db/atomic-batch-payment-migration.test.ts` — 4 passed, 0 failed, 23 expectations. Real PostgreSQL now rejects inserting or moving a batch member under a posted parent; the guard checks both OLD and NEW membership, not only the old parent.
- `./scripts/test-disposable-postgres.sh src/services/payment-batch-staging.integration.test.ts src/db/atomic-batch-payment-migration.test.ts` — 13 passed, 0 failed, 64 expectations. Staging retry, owner access, immutable ready evidence, expiry/MIME/size metadata, changed review payload, append-only receipts, and revision-bound cancellation all passed.
- `cd frontend && bun run test` — 62 files, 274 tests passed after enabling `src/**/*.{test,vitest}.{ts,tsx}` discovery and repairing the discovered Bun-only clock import/assertions.
- `cd frontend && bun run lint` — passed.
- `cd frontend && bun run build` — passed; existing chunk-size warning only.
- `cd plugins/creditsync && bun test && bun run validate` — 56 tests passed; validator passed (9.2.0, 11 skills, 116 tools).
- `cd backend && bun run typecheck` — passed after cancellation/schema/UI-adjacent backend changes.
- `./scripts/test-disposable-postgres.sh src/services/floating-allocation-regressions.test.ts` — 21 passed, 0 failed, 122 expectations after preserving component-specific penalty compensation errors; chronology protection remains enforced by the later allocation guard.
- `cd frontend && bun run test -- --run src/pages/dashboard/payments/PaymentBatchEditor.test.tsx` — 1 passed; `bun run lint` passed; `bun run build` passed with the existing >500 kB chunk warning. The preview path now uses same-call staging results, covering resumable retry semantics that React state updates cannot guarantee synchronously.
- `cd backend && bun run typecheck` — passed after the floating error-precedence and resumable preview fixes.
- Correction 1: the disposable runner now uses `--parallel=1` because `--max-concurrency=1` does not serialize test files; this addresses the observed 40P01 cross-worker reset cycle. The two named service files passed together 36/36 and 143 expectations. The subsequent full rerun was blocked by Docker/PostgreSQL `No space left on device` after 850 pass / 3 skip / 56 cascading DB-write failures; no financial assertion was relaxed. Evidence: `/tmp/creditsync-batch-resume-correction1-rerun2/`.
- Cleanup correction: the runner now creates one generated, labeled volume per invocation, records its exact name, and removes that volume plus its exact container in the EXIT trap. Migration smoke passed 4/4 with 23 expectations; failure-path smoke reached Bun's invalid-filter failure and left zero labeled containers/volumes. Logs: `post-cleanup-smoke.log` and `post-cleanup-failure.log`.
- User-resume full suite at HEAD `98b80b0`: 905 passed, 3 skipped, 1 failed. The single failure exposed principal reprojection running before the shared later-floating-payment guard. Added the guard before principal reprojection; `./scripts/test-disposable-postgres.sh src/services/payment-service.test.ts` then passed 37/37 with 185 expectations. Full suite must be rerun at the resulting commit.

Known acceptance gaps at this checkpoint: staging retry/cross-tenant/partial-finalize DB cases; warning acknowledgement persistence and all-borrower chronology; shuffled 09/07/08 and 75+45 multi-contract assertions; restore new-evidence end-to-end; MCP frozen contract/plugin sync; frontend four-step UI and discovered tests; full final verification.

The latest checkpoint closes the specifically named staging/posted-parent and frontend discovery gaps, but the execution log remains incomplete: temporal reflow, full component replay, split/dependency public APIs, candidate/access completeness, all-writer chronology audit, frozen contract synchronization, browser QA, and final full-suite result are still open.

## TDD evidence

Red/green commands will be appended per task before implementation claims.

### 2026-09-09 — user-resume workspace continuation

- Workspace RED: `backend/scripts/test-disposable-postgres.sh src/services/payment-batch-staging.integration.test.ts` failed because the new `getPaymentBatchWorkspace` service export did not exist.
- Workspace GREEN: the same serialized disposable test passed 10/10 with 46 expectations. The API returns staging public IDs, client keys, lifecycle revision, normalized amount/time when reviewed, evidence status/metadata, review reason/range, and public intake/batch-item links; raw file IDs, storage keys, hashes, signed URLs, and OCR contents are excluded.
- The full serialized suite at commit `310e088` remains the latest backend gate: 906 passed, 3 skipped, 0 failed, 5799 expectations. This continuation's workspace change requires a new full-suite run before final acceptance.
- At commit `a39daa0`, the first post-workspace full suite exposed one 5-second timeout in the intermediary remittance selection test (`906 pass / 3 skip / 1 fail / 1 error`, 5801 expectations). The exact file then passed in three isolated disposable runs (6/6 each, 0.93–1.51s), without changing timeout or financial assertions. A clean serialized rerun at the same HEAD passed `907 / 3 skipped / 0 failed`, 5804 expectations, 910 tests in 210.46s; no owned container or volume remained.
- Final non-DB gates at `a39daa0`: `cd backend && bun run typecheck` passed; `cd frontend && bun run test` passed 274 tests in 62 files, lint passed, and build passed with the existing chunk-size warning; `cd plugins/creditsync && bun test` passed 56 tests/1688 expectations and `bun run validate` passed (9.2.0, 11 skills, 116 tools).

### 2026-09-10 — resumable staging edit and split continuation

- TDD RED: `./scripts/test-disposable-postgres.sh src/services/payment-batch-staging.integration.test.ts` failed at module loading because `editPaymentBatchStagingItem` and `splitPaymentBatch` did not exist.
- GREEN: the same serialized disposable suite passed 12/12 with 59 expectations. It covers revision/idempotency-bound draft edits, stale revision rejection, zero transactions, unresolved staging split, exact replay metadata, membership preservation, and no duplicated intake/evidence.
- Regression GREEN: `./scripts/test-disposable-postgres.sh src/services/payment-batch-staging.integration.test.ts src/db/atomic-batch-payment-migration.test.ts src/services/payment-batch-service.test.ts` passed 23/23 with 102 expectations. `cd backend && bun run typecheck` passed.
- Added additive migration `0067_batch_staging_edits_and_dependencies.sql`, tenant-composite source/destination FKs, immutable split provenance metadata, REST edit/split schemas, and chronology-aware preview/execute checks. This task commit is not full seven-task acceptance; chronology, accounting/reflow, UI/MCP synchronization, and final verification remain open.

### 2026-09-10 — supervisor defect correction

- `staging-completion-brief.md` was requested by the supervisor but is absent from this worktree and reachable Git history; the approved plan and supervisor defect list were used as the available authority.
- TDD RED reproduced reviewed-item split orphaning, queued-lock stale revision acceptance, loan-only mapping authorization bypass, and mapping preview omission. GREEN: `./scripts/test-disposable-postgres.sh src/services/payment-batch-staging.integration.test.ts src/db/atomic-batch-payment-migration.test.ts src/services/payment-batch-service.test.ts src/services/payment-chronology-guard.test.ts` passed 35/35 with 0 failures; `cd backend && bun run typecheck` passed.
- Fixed source reload after `FOR UPDATE`, canonical staging/batch-item membership moves, immutable split provenance without an unconditional source prerequisite, mapping access/schedule validation and preview consumption, mapping clear-vs-omit hashing, and borrower locking before explicit mapping edits. Full seven-task acceptance remains unfinished.

### 2026-09-10 — supervisor fix round two

- TDD disposable regressions cover mixed mapped/unmapped fallback, secondary borrower mapping, UTC-midnight Bangkok date conversion, old/current/requested borrower lock coverage, known mapped staging without an intake, review propagation, and tagged staging/item membership identities.
- Targeted serialized result: `./scripts/test-disposable-postgres.sh src/services/payment-batch-staging.integration.test.ts src/services/payment-chronology-guard.test.ts src/services/payment-batch-atomic.integration.test.ts` — 38 passed, 0 failed; `cd backend && bun run typecheck` — passed.
- Runtime split provenance remains immutable and separate from chronology blocking; source/destination are not unconditionally ordered. Full ready-prefix split-direction execution coverage, broader UI/MCP/reconciliation work, and final full-plan verification remain supervisor gaps.

### 2026-09-10 — supervisor fix round three

- TDD RED: the new multi-borrower review regression failed with `BATCH_ALLOCATION_MISMATCH`, proving review incorrectly treated the batch header borrower as exclusive. A queued review mapping-drift regression was added and requires rejection before any intake write.
- Fixes: review now compares preliminary and post-lock mapping/revision/header state, revalidates borrower portfolio access, uses the authoritative post-lock loan for intake provenance, and leaves batch borrower resolution per item. Edit and preview apply the same post-lock drift boundary; explicit allocations are checked in full against every reviewed mapping; pending chronology includes reviewed mappings after intake creation while excluding only the current intake set.
- Serialized disposable verification: `./scripts/test-disposable-postgres.sh src/services/payment-batch-staging.integration.test.ts src/services/payment-chronology-guard.test.ts src/services/payment-batch-atomic.integration.test.ts` — 41 passed, 0 failed, 128 expectations; `cd backend && bun run typecheck` — passed. Ready-prefix regression executed and posted the older selected item while the later selected portion remained blocked; no later transaction was created.
- Round-three scope is complete on this branch, but the seven-task plan remains unfinished: reconciliation/reflow, broader UI/MCP synchronization, and final full-plan verification remain open.

### 2026-09-10 — authoritative per-item borrower constraints

- Supervisor timeout was reproduced as a single staging test: 1 passed, 0 failed in 1.64s including migration (test body 419ms); this was resource/startup variance, not a deadlock, and no timeout/assertion was changed.
- TDD RED: borrower-only staging mappings were reproduced allowing an explicit allocation to another borrower; the preview failed later for the wrong reason, confirming the mapping was not enforced at the planning boundary. Solver RED also reproduced a unique 75+45 sum returning `ready` and a Bangkok 17:00 UTC transfer being treated as future.
- Added borrower constraints to solver eligibility, retained the existing accounting planner, validated all explicit allocations against the reviewed borrower (including multi-contract allocations), grouped chronology by solved or reviewed per-item borrower, sorted planning slips by timestamp, and held unique multi-contract sums for human selection. Unresolved mappings remain held; they are not expanded to other borrowers or tenant-wide locks.
- Final serialized verification: `./scripts/test-disposable-postgres.sh src/services/payment-batch-staging.integration.test.ts src/services/payment-chronology-guard.test.ts src/services/payment-batch-atomic.integration.test.ts` — 43 passed, 0 failed; `bun test src/services/payment-batch-solver.test.ts` — 5 passed, 0 failed; `cd backend && bun run typecheck` — passed; `git diff --check` — passed. Full seven-task completion remains open.

### 2026-09-10 — current authority and candidate cardinality checkpoint

- TDD RED reproduced the remaining solver defect: a `120.00` slip became `ready` with eligible loans `120.00` and `80.00`; RED also reproduced the public remap chronology failure where the old batch header still blocked the remapped borrower.
- Current staging mappings now suppress stale proposal/origin/header chronology fallbacks, reviewed borrower-only mappings do not inherit another queried borrower's batch header, and public staging edits stale active match proposals as well as batch previews. The solver now counts distinct eligible loan contracts before subset inference, keeps `120/80` and cross-contract `75+45` candidates in human review, preserves same-loan multi-schedule planning, and uses Bangkok-normalized timestamps.
- Final focused serialized verification: `./scripts/test-disposable-postgres.sh src/services/payment-batch-staging.integration.test.ts src/services/payment-chronology-guard.test.ts src/services/payment-batch-atomic.integration.test.ts` — 44 passed, 0 failed, 139 expectations; `bun test src/services/payment-batch-solver.test.ts` — 6 passed, 0 failed, 17 expectations; backend typecheck and `git diff --check` passed. The timeout reproduction remains 1 pass in 1.64s with no timeout change. Full plan, UI/MCP synchronization, reflow, and broader acceptance remain open.

### 2026-09-10 — resolution-state distinction checkpoint

- TDD RED reproduced the coarse exclusion: a reviewed intake with no staging mapping but a current explicit batch preview for borrower B disappeared from chronology, allowing a later B batch preview. RED setup used public staging, evidence finalization, review, explicit batch preview, and later capture.
- Resolution authority now distinguishes current reviewed mapping, current ready batch allocation at the batch revision, and never-mapped/header capture state. Stale match proposals and previews are excluded; public edits continue to invalidate active match proposals and batch previews. Chronology inspection uses the same current ready batch allocation rule rather than falling back to the owner header when an item is currently resolved elsewhere.
- Added additive migration `0068_staging_resolution_state.sql` with revision-bound `unresolved`/`mapped`/`cleared` staging state; no posted history is changed. Focused public regressions cover: never-mapped header behavior, map-A/preview/remap-B with later A allowed and later B blocked, explicit preview-B without staging mapping blocking later B, clear-after-preview invalidating the old preview and leaving the cleared source unresolved, and public edit invalidating the old match proposal. Serialized DB verification: 46 passed, 0 failed, 147 expectations; solver: 6 passed, 0 failed, 17 expectations; typecheck and diff-check passed. README was unchanged because the existing workflow description already documents mapping-aware invalidation and chronology. Full plan remains open.
- Final shared-guard regression added a direct `assertNoOlderPendingPayment` assertion and a standalone ready-proposal post attempt; both reject chronology for a current explicit batch allocation with zero transactions. Latest focused serialized verification remains 46 passed, 0 failed, now 151 expectations; backend typecheck passed. Full plan remains open.

### 2026-09-10 — Tasks 1/2/4 clear transition and writer matrix checkpoint

- TDD RED reproduced clear-state auto-reselection: after public `mapping: null`, a new unmapped preview silently selected the previous eligible loan. The fix holds a cleared staging item unless the caller supplies a new explicit allocation; explicit reselection creates a current ready preview and later same-borrower payment is blocked. The old preview execute remains stale and creates zero transactions.
- Shared financial-writer matrix from current callers:

  | Writer/caller | Transaction boundary and lock order | Chronology/snapshot/append-only status |
  | --- | --- | --- |
  | `postPayment` / allocation kernel | DB transaction; borrower locks, intake, then loan/schedule | older-pending and later-floating checks; proposal/state recheck; append-only transaction/ledger |
  | `executePaymentBatch` | DB transaction; ordered borrowers, batch, intakes, loans/schedules | current preview/confirmation/state checks; per-item chronology; atomic append-only writes |
  | `reversePayment` | DB transaction; borrower then intake/loan | posted target and downstream ordering checks; compensating transaction/ledger |
  | reconciliation execute / restore execute | DB transaction; borrower set from intake/allocation before intake/loan locks | authorized preview/proposal and balance checks; restore/reconciliation append-only records; shared chronology coverage remains partial |
  | `postPaymentAllocationInTransaction` | caller-owned transaction; caller owns borrower/intake/loan locks | reuses posting kernel, but caller-specific chronology responsibility must remain explicit |
  | renewal/restructure/settlement execute and reversals | service-owned DB transactions with service-specific loan/borrower locks | immutable preview/state and compensating history; shared pending-payment race coverage is not yet uniform |

- Focused serialized verification: staging/chronology/atomic `46 passed, 0 failed, 156 expectations`; migration constraints `4 passed, 0 failed, 23 expectations`; backend typecheck passed. Fresh disposable migration applies 0068 and verifies posted-member immutability; an isolated 0067-to-0068 upgrade fixture with populated draft/posted rows remains a supervisor gap. UI/MCP synchronization, all-writer concurrency expansion, and temporal reflow remain open.

### 2026-09-10 — populated 0067-to-0068 upgrade gate

- Added `atomic-batch-payment-migration.test.ts` coverage in the default serialized disposable runner. The fixture populates mapped and legacy-unresolved staging rows, a posted batch/member/preview/allocation, ready staging evidence, a loan schedule, a payment intake, and an immutable transaction; it then removes only the 0068-owned column in the disposable database and applies the exact committed `0068_staging_resolution_state.sql`. Historical migrations and checksums are not edited.
- Verified mapped legacy rows backfill to `mapped`, null legacy mappings remain `unresolved` (never inferred as `cleared`), the new check constraint and existing received-time index exist, invalid state is rejected, economic transaction fields/dates/components, posted membership, and ready evidence snapshots are unchanged, and posted batch/member/evidence mutations are rejected. A second migrator call is a no-op: staging row count/metadata and `drizzle.__drizzle_migrations` identities, order, hashes, and timestamps remain unchanged.
- RED fixture run initially failed when attempting to create a member under an already-posted parent; this was corrected by constructing the complete historical fixture before transitioning the parent to posted, retaining the immutability assertion. GREEN focused upgrade: `5 passed, 0 failed, 39 expectations` (`.codex-task-logs/tasks124-populated-upgrade-green.log`). Combined serialized staging/chronology/atomic/migration run: `51 passed, 0 failed, 195 expectations` (`.codex-task-logs/tasks124-populated-focused.log`). Backend typecheck passed (`.codex-task-logs/tasks124-populated-typecheck.log`).
- The follow-up real-migrator gate now uses an owned scratch database: a copied prefix of the committed ordered migration set runs through 0067, confirms 0068 is absent from both schema and journal, then the full copied set applies 0068 and is rerun. This is an actual prefix/full migrator proof, not a raw-SQL column simulation; it does not claim every historical production-data variant. Real gate: `.codex-task-logs/tasks124-real-upgrade-green-2.log` (`6 passed, 0 failed, 52 expectations`); final combined gate: `.codex-task-logs/tasks124-real-upgrade-focused-final.log` (`52 passed, 0 failed, 208 expectations`); final typecheck: `.codex-task-logs/tasks124-real-upgrade-typecheck-final.log`. All-writer concurrency, UI/MCP synchronization, temporal reflow, and full-plan gates remain open.

### 2026-09-10 — restore/reconciliation versus ordinary posting checkpoint

- Verified actual callers and lock order: restore/reconciliation preview reads source/reversal/evidence/provenance and balance snapshots in a DB transaction; execute acquires the deterministic borrower set first, then proposal/intake/loan/accrual/transaction locks, re-reads source/provenance/balance, and only then appends compensation/replacement rows. Ordinary `postPayment` uses the same borrower-first chronology boundary and accounting kernel. `reversePayment` also uses borrower-first then intake/loan/transaction order. `postPaymentAllocationInTransaction` remains caller-transaction-owned. Renewal/restructure/settlement and historical non-restore reconciliation remain outside this checkpoint.
- Added deterministic barrier coverage in `payment-restore-floating.integration.test.ts`: borrower row lock observation through `pg_stat_activity` proves restore waits before mutating the child; an ordinary floating post after restore preview causes `STALE_RECONCILIATION_PREVIEW` with draft child and zero child transactions; the existing same-key retry verifies one append-only result. Unsupported floating penalty provenance remains fail-closed.
- Final serialized commands/results: `./scripts/test-disposable-postgres.sh src/services/payment-restore-floating.integration.test.ts` — `8 passed, 0 failed, 28 expectations` (`.codex-task-logs/tasks245-final-restore.log`); combined migration/restore/atomic gate — `28 passed, 0 failed, 149 expectations` (`.codex-task-logs/tasks245-final-focused.log`); `cd backend && bun run typecheck` passed (`.codex-task-logs/tasks245-final-typecheck.log`); `git diff --check` passed (`.codex-task-logs/tasks245-final-diffcheck.log`).
- Writer matrix status: `postPayment`/batch atomic/reversal — covered by existing focused atomic tests; `executePaymentReconciliation` exact-restore path — covered by the two new barrier/race tests plus retry; `previewPaymentRestore` — covered by existing restore suite; `previewPaymentReconciliation` historical explicit-allocation path — not exercised here; restore backfill — not exercised here; reconciliation/restore REST and MCP adapters — route parity is inspected but adapter-level concurrency is pending; renewal/restructure/settlement/adjustment writers — pending. Full all-writer, UI/MCP, temporal reflow, and plan gates remain open.
- This checkpoint extends the matrix: `backfillPostedRestoreSchedule` now resolves borrowers from source and child, locks sorted borrowers before intake/loan/transaction/schedule rows, re-reads after the borrower boundary, rejects borrower-scope expansion, and updates only the derived schedule projection. Its deterministic barrier test proves the schedule remains unchanged while the borrower is held. The historical explicit reconciliation service was read and its existing execute borrower-lock/revision guards remain unchanged; no new public historical-allocation barrier was added because the available disposable fixture does not safely expose a second authorized compensation plan without broadening scope.
- Final checkpoint evidence: `src/services/payment-reconciliation-service.test.ts` — `16 passed, 0 failed, 73 expectations` (`.codex-task-logs/tasks245-reconciliation-final.log`); `src/services/payment-restore-floating.integration.test.ts` — `8 passed, 0 failed, 28 expectations` (`.codex-task-logs/tasks245-restore-green.log`); combined serialized reconciliation/restore/migration/atomic gate — `44 passed, 0 failed, 194 expectations` (`.codex-task-logs/tasks245-final-focused-2.log`); backend typecheck passed (`.codex-task-logs/tasks245-typecheck-final.log`); `git diff --check` passed (`.codex-task-logs/tasks245-diffcheck-final.log`). The earlier RED is retained in `.codex-task-logs/tasks245-backfill-red.log` (`15 passed, 1 failed`) and demonstrates the missing borrower wait. Migration real-prefix proof remains from `9e57809` and is not rerun in this bounded writer checkpoint. Renewal/restructure/settlement, UI/MCP synchronization, temporal reflow, and full-plan gates remain open.
- Follow-up bounded receipt/reconciliation checkpoint: added additive `0069_generic_command_receipts.sql` with tenant+operation uniqueness, audit linkage, and append-only mutation trigger. `backfillPostedRestoreSchedule` now validates tenant access before receipt replay, fingerprints target/reason, serializes the key namespace, and stores the exact result with its audit/correlation for changed and no-op commands. RED replay/conflict evidence is `.codex-task-logs/tasks246-receipt-red.log`; GREEN reconciliation is `17 passed, 0 failed, 79 expectations` (`.codex-task-logs/tasks246-reconciliation-green-2.log`). A genuine non-restore historical explicit-interest reconciliation barrier test proves borrower wait before intake/loan locks and one replacement transaction after release. Combined final gate is `45 passed, 0 failed, 200 expectations` (`.codex-task-logs/tasks246-final-focused.log`); backend typecheck passed (`.codex-task-logs/tasks246-typecheck.log`). Remaining restore/reconciliation adapter and other-writer/UI/MCP/reflow gates remain open.
- Receipt/reconciliation implementation was reverified at the final working tree: `17 passed, 0 failed, 79 expectations` (`.codex-task-logs/tasks246-reconciliation-green-3.log`), including same-key replay/conflict, initial no-op receipt, borrower barrier backfill, and non-restore historical explicit barrier; combined serialized gate `45 passed, 0 failed, 200 expectations` (`.codex-task-logs/tasks246-final-focused-2.log`); backend typecheck passed (`.codex-task-logs/tasks246-typecheck-2.log`); `git diff --check` passed (`.codex-task-logs/tasks246-diffcheck.log`). No posted financial transaction or historical record is rewritten by the backfill path. Other principal writers, adapters, UI/MCP, temporal reflow, and full-plan gates remain open.
- Final receipt/reconciliation gate: `backfillPostedRestoreSchedule` now uses `command_receipts` with tenant+operation uniqueness and exact replay result, including initial no-op; the public test covers concurrent identical keys for both mutation and no-op, different target/reason conflict, cross-tenant access denial, receipt update/delete rejection, and invalid audit FK. The historical non-restore tests `historical explicit reconciliation waits on borrower lock before intake or loan locks` and `historical explicit reconciliation rejects an ordinary posting after preview without compensation` now prove scoped borrower waiting, intake/loan `FOR UPDATE NOWAIT`, exact same-key replay without duplicate transaction, stale rejection, and zero source compensation. Final serialized gate: `46 passed, 0 failed, 216 expectations` (`.codex-task-logs/tasks247-final-focused-4.log`); reconciliation subset: `18 passed, 0 failed, 95 expectations` (`.codex-task-logs/tasks247-reconciliation-final-4.log`); typecheck (`.codex-task-logs/tasks247-typecheck-final-4.log`) and diff-check (`.codex-task-logs/tasks247-diffcheck-final-4.log`) passed. Public lock-set expansion after waiting remains untestable without a supported public mutation that can alter an immutable reconciliation proposal/source during the borrower wait; the execute path still rejects reloaded borrower expansion before downstream locks. Principal writers, UI/MCP, temporal reflow, and full-plan gates remain open.

### 2026-09-10 — loan renewal shared-lock checkpoint

- TDD RED: `src/services/loan-renewal-service.test.ts` failed with `renewal operation did not wait on the borrower row lock`, reproducing the old loan-first acquisition order.
- Renewal preview, execute, and reverse now use sorted borrower-first locks before loan/renewal/replacement-loan locks, re-read borrower identity after the lock boundary, reject replacement borrower lock-set expansion, and run the shared older-pending chronology guard before preview/execute snapshots. Renewal accounting formulas and append-only history are unchanged.
- Added a deterministic borrower-lock barrier with loan and renewal `FOR UPDATE NOWAIT` proof, plus a sequential same-key replay assertion. Corrected the reconciliation barrier to announce its blocker PID only after borrower `FOR UPDATE`; execution is always awaited in `finally`.
- Final serialized focused gate: `src/services/loan-renewal-service.test.ts`, `src/services/payment-batch-atomic.integration.test.ts`, `src/services/payment-reconciliation-service.test.ts`, and `src/services/payment-restore-floating.integration.test.ts` — 70 passed, 0 failed, 289 expectations (`.codex-task-logs/tasks248-focused-final.log`). Renewal subset GREEN: 30 passed, 0 failed, 124 expectations (`.codex-task-logs/tasks248-renewal-green.log`). Backend typecheck and `git diff --check` passed (`.codex-task-logs/tasks248-typecheck.log`, `.codex-task-logs/tasks248-diffcheck.log`). Renewal reversal/ordinary-post lock races beyond this barrier and restructure/settlement, UI/MCP, temporal reflow, and full-plan gates remain open.

### 2026-09-10 — restructure and settlement shared-lock checkpoint

- TDD RED: new deterministic barrier tests failed `2/39` because restructure and settlement execution did not wait on the borrower row; both could acquire loan/proposal locks while the borrower was held.
- Restructure and settlement preview/execute/reversal now lock sorted borrower IDs before loan, proposal, intake, disbursement, schedule, transaction, and accrual rows; preview computations run after the borrower/loan re-read, borrower identity changes fail closed, and the shared older-pending guard runs against the effective Bangkok date before financial snapshots.
- Added genuine borrower-wait barriers with blocker PID announced after `FOR UPDATE`, loan/proposal `NOWAIT` proof, cleanup awaiting in-flight promises, and a public renewal-preview followed by ordinary payment regression that rejects stale renewal execution without a replacement loan or extra transaction. Existing append-only/idempotency/downstream reversal guards remain intact.
- Structural serialized command: `./scripts/test-disposable-postgres.sh src/services/loan-restructure-service.test.ts src/services/loan-settlement-service.test.ts` — `39 passed, 0 failed, 164 expectations` (`.codex-task-logs/tasks249-green-structural.log`); renewal race subset — `31 passed, 0 failed, 128 expectations` (`.codex-task-logs/tasks249-renewal-race.log`). Backend typecheck passed (`.codex-task-logs/tasks249-typecheck-first.log`). Combined payment atomic verification remains required before checkpoint review. UI/MCP, temporal reflow, and remaining principal-writer coverage remain open.

### 2026-09-10 — Task 6 backend public workflow contract checkpoint

- TDD RED: the settlement replay regression failed after inserting a newly arrived older intake with an authoritative `originLoanId`; retry was incorrectly stopped by chronology instead of returning the committed receipt. Moving the new-execution chronology guard after the exact executed-replay branch made the focused settlement suite pass `23/23` with `78` expectations. Restructure received the same ordering fix, and reversal now validates the current replacement borrower before downstream loan locks.
- Exposed the existing tenant-safe batch services through authenticated REST for workspace, staging evidence/review/edit, split, decision, cancel, preview, and execute; added direct MCP handlers and closed request schemas for workspace, review, edit, split, decision, and cancel. MCP calls the application services directly and does not call REST. Existing borrower search/portfolio and backend preview remain the candidate/accounting sources; no agent-side fuzzy mapping or calculator was added.
- Synchronized the plugin frozen contract from the actual MCP `tools/list`: plugin `9.2.0`, 11 skills, 122 tools. Plugin tests passed `56/56` with `1688` expectations and validator passed; evidence: `.codex-task-logs/tasks251-plugin-test-final.log` and `.codex-task-logs/tasks251-plugin-validate.log`.
- Contract/unit verification after the REST/MCP additions passed `35/35` with `530` expectations (`.codex-task-logs/tasks251-contract-green.log`). Required DB-focused staging/chronology/atomic verification and final backend typecheck remain to be run at the commit HEAD. UI four-step implementation, OCR/human-review integration, temporal reflow, planner edge completion, and full verification remain open.

### 2026-09-10 — Task 6 REST/MCP resumable staging contract checkpoint

- Added direct MCP handlers for upload-first `payment.batch.stage` and staging evidence `prepare`/`finalize`, reusing existing application services and the evidence gateway. Batch preview allocations now accept optional borrower and nullable/omitted schedule identifiers for floating and multi-borrower planning, plus optional chronology `decisionPublicId`; strict workspace/staging/review/decision/cancel/preview schemas expose only validated public fields.
- Closed the corresponding authenticated REST request schemas, including explicit unknown-field rejection and canonical money/date/time/UUID validation. A real default MCP integration covers stage, workspace, evidence prepare/finalize, review, preview, decision, re-preview, workspace latest-preview inspection, execute, and exact replay. Split/edit/cancel remain covered by service and contract tests, not claimed by this end-to-end case.
- Synchronized the private plugin to `9.3.0`, 11 skills, and 125 tools from the actual backend advertisement; regenerated the frozen contract, added the resumable workflow eval/guidance, and updated root/plugin workflow docs. Named candidate discovery was not added; borrower search/portfolio and backend accounting remain authoritative. UI/OCR/human review, reflow, and full-plan gates remain open.
- TDD RED is retained in `.codex-task-logs/tasks252-contract-red.log`. Contract tests: `33/33, 531 expectations` (`.codex-task-logs/tasks252-server-green.log`); REST schema: `3/3, 4 expectations` (`.codex-task-logs/tasks252-rest-schema-green.log`); real MCP flow: `1/1, 32 expectations` (`backend/.codex-task-logs/tasks252-real-mcp-final2.log`); focused serialized DB: `55/55, 737 expectations` (`backend/.codex-task-logs/tasks252-focused-db-final2.log`); plugin tests: `56/56, 1708 expectations` (`.codex-task-logs/tasks252-plugin-test-final2.log`); validator passed (`.codex-task-logs/tasks252-plugin-validate-final2.log`); backend typecheck passed (`.codex-task-logs/tasks252-final-typecheck3.log`).

### 2026-09-10 — Task 3/6 named borrower and contract candidate discovery checkpoint

- Added read-only `discoverPaymentBatchCandidates` over the existing tenant/portfolio borrower search and backend accounting projections. It accepts a reviewed staging item (or explicit reviewed amount/time), returns bounded named borrower candidates and public contract candidates with repayment type/status, Bangkok business date, exact decimal balances/components, schedule details, and explicit eligibility/review flags. It never creates intake, evidence, accrual, ledger, or financial rows; fuzzy matches remain candidates only and multiple eligible contracts remain human selection.
- Exposed the same service as authenticated REST `GET /payment-batches/staging/:stagingItemPublicId/candidates` and direct MCP `payment.batch.candidates` with closed UUID/date/money inputs and strict public outputs. The real MCP staging integration now calls candidate discovery before preview. Plugin contract synchronized to version `9.4.0`, 11 skills, 126 tools, with eval, validator, skills, and docs updated.
- TDD RED for the absent candidate service is `.codex-task-logs/tasks253-candidate-red.log`; the disposable candidate service test passed `1/1, 4 expectations`, and the real default MCP workflow plus candidate discovery passed `19/19, 640 expectations` (`backend/.codex-task-logs/tasks253-focused-final.log`), including a shuffled two-date floating two-contract preview with explicit 75+45 allocations, missing-day decision, atomic execute, and exact replay. REST module schemas passed `3/3, 4 expectations` (`backend/.codex-task-logs/tasks253-rest-strict-final2.log`) with the candidate response schema; MCP contract tests passed `33/33, 531 expectations` (`backend/.codex-task-logs/tasks253-server-final.log`). Plugin tests passed `56/56, 1710 expectations` (`.codex-task-logs/tasks253-plugin-test-final2.log`); validator passed (`.codex-task-logs/tasks253-plugin-validate-final2.log`); backend typecheck and diff-check passed (`.codex-task-logs/tasks253-typecheck-strict-final2.log`, `.codex-task-logs/tasks253-diffcheck-strict-final2.log`). Candidate/access breadth beyond these synthetic cases and UI/OCR integration remain open; no four-step UI completion is claimed.
- Follow-up candidate safety RED reproduced missing revision metadata and the access-boundary regression. Candidate output now includes staging/batch revisions and an input fingerprint, filters contracts by the same portfolio ownership policy, preserves ambiguous names, and separates authoritative scheduled/floating due components from the input-specific proposal projection. Focused candidate integration passed `2/2, 12 expectations` (`backend/.codex-task-logs/tasks254-candidate-green5.log`); real MCP default flow passed `18/18, 636 expectations` (`backend/.codex-task-logs/tasks254-default-final.log`); REST schema/typecheck/diff-check passed (`backend/.codex-task-logs/tasks254-rest-final5.log`, `backend/.codex-task-logs/tasks254-typecheck-final3.log`, `backend/.codex-task-logs/tasks254-diffcheck-final3.log`); plugin tests passed `56/56, 1710 expectations` and validator passed (`.codex-task-logs/tasks254-plugin-final2.log`, `.codex-task-logs/tasks254-validator-final2.log`). UI/OCR, temporal reflow, broader candidate pagination/access matrix, and full-plan verification remain open.

### 2026-09-10 — Task 6 bounded four-step batch workspace UI checkpoint

- TDD RED is retained in `.codex-task-logs/tasks255-ui-red.log`; it reproduced the old manual borrower-UUID editor and missing upload/review control. The model/editor follow-up is `.codex-task-logs/tasks256-ui-red2.log` and `.codex-task-logs/tasks256-ui-green4.log`.
- Replaced the UUID-entry editor with a bounded capture/review/chronology/confirmation workspace: multi-file 1–50 selection/drop, stable client keys, resumable workspace hydration, evidence prepare/PUT/finalize and review without duplicate intake, named borrower/contract candidate selectors, explicit multi-contract allocation rows, backend preview/decision refresh, latest-preview confirmation, stable execute replay key, and explicit split/cancel controls. UI formatting uses Decimal-backed model helpers and Bangkok date rendering; no OCR provider or financial calculator was added, and draft/review actions remain unposted.
- Frontend verification: focused model/editor tests `8 passed, 0 failed` (`.codex-task-logs/tasks256-ui-green4.log`); full Vitest `62 files, 277 passed` (`.codex-task-logs/tasks256-frontend-full-test.log`); lint passed (`.codex-task-logs/tasks256-frontend-lint.log`); production build passed with the existing chunk-size warning (`.codex-task-logs/tasks256-frontend-build3.log`). Browser smoke reached the synthetic local landing page but could not enter the authenticated dashboard because no Google client configuration/auth tenant was available; no file or financial data was transmitted. The prior candidate default-MCP timeout remains an environment/slow cumulative integration issue: 19 passed and one established adapter test exceeded its 10s budget at 10.697s in `/tmp/creditsync-batch-resume.cs2Qp2/supervisor-candidates.log`; it was not skipped or weakened. Candidate discovery's global read snapshot and broader real authenticated browser/API end-to-end remain gaps. Task 6, temporal reflow, OCR integration, and full-plan gates remain open.
- Fix follow-up TDD RED/green evidence: `.codex-task-logs/tasks257-ui-integration.log` captured the initial malformed integration fixture; `.codex-task-logs/tasks257-ui-integration5.log` is the corrected component path with 11 passing focused tests. The UI now separates capture/evidence from review metadata, persists scoped capture/execute keys and safe draft selections, preserves server client keys/revisions/evidence status on hydration, reuses ready evidence, sends returned decision IDs into fresh previews, exposes audited reviewed-item edits, and prevents implicit contract selection. Full frontend Vitest passed `62 files, 280 tests` (`.codex-task-logs/tasks257-frontend-full.log`), focused build passed (`.codex-task-logs/tasks257-ui-build2.log`), lint passed (`.codex-task-logs/tasks257-ui-lint2.log`); the earlier browser auth blocker remains at `.codex-task-logs/tasks256-browser-blocker.log`. Remaining gaps are authenticated browser E2E, OCR provider integration, complete public split/receipt result rendering, and temporal reflow/full-plan gates.
- UI state/provenance follow-up: Bangkok `datetime-local` values now fail closed and normalize before candidate requests; late candidate/preview responses cannot restore invalidated state; reviewed edits use a stable revision-bound command key; receipt public identifiers are shown after execute; workspace drafts and keys remain tenant/actor scoped. Focused editor/model tests passed `11/11` (`.codex-task-logs/tasks258-ui-focused3.log`), full frontend Vitest `62 files, 280 tests` (`.codex-task-logs/tasks258-frontend-full2.log`), build/lint/typecheck/diff-check passed (`.codex-task-logs/tasks258-build2.log`, `.codex-task-logs/tasks258-lint2.log`, `.codex-task-logs/tasks258-backend-typecheck.log`, `.codex-task-logs/tasks258-diffcheck.log`). A deliberately discarded decision integration fixture did not reflect the server workspace lifecycle; the returned decision ID is implemented but that path remains an integration-test gap. Authenticated browser E2E, OCR integration, full split/cancel receipt rendering, temporal reflow, and full-plan gates remain open.
- Follow-up RED/GREEN: `.codex-task-logs/tasks259-red-bangkok.log` reproduced acceptance of `24:00`; `.codex-task-logs/tasks259-green-focused2.log` and `.codex-task-logs/tasks259-green-editor3.log` passed focused editor/model coverage. Workspace hydration now inherits local metadata only for the same `clientItemKey`, never by array index; missing evidence/upload URL remains fail-closed; resumed pending rows accept file reselection; reviewed edits mark an unsaved gate until the audited server edit refreshes state. Default frontend Vitest passed `62 files, 280 tests` (`.codex-task-logs/tasks259-frontend-full-final.log`). Build/lint/backend typecheck passed (`.codex-task-logs/tasks259-build.log`, `.codex-task-logs/tasks259-lint.log`, `.codex-task-logs/tasks259-backend-typecheck.log`); full browser/authenticated end-to-end, split/cancel destination/receipt rendering, OCR, temporal reflow, and full-plan gates remain open.
- UI split/preview follow-up: TDD RED for public preview components is `.codex-task-logs/tasks260-preview-components-red.log`; serialized atomic GREEN passed `14/14, 42 expectations` (`.codex-task-logs/tasks260-preview-components-green.log`). Preview now returns backend-calculated component strings through the strict MCP contract, and the UI renders identifiable candidate labels, chronological sequence, and split destination/dependency membership with resume navigation. Focused editor/model passed `11/11` (`.codex-task-logs/tasks260-ui-focused.log`); full frontend passed `62 files, 280 tests` (`.codex-task-logs/tasks260-frontend-full.log`); build, lint, backend typecheck, plugin `56/56, 1710 expectations`, and validator passed (`.codex-task-logs/tasks260-build.log`, `.codex-task-logs/tasks260-frontend-lint-final2.log`, `.codex-task-logs/tasks260-backend-typecheck.log`, `.codex-task-logs/tasks260-plugin-test-final.log`, `.codex-task-logs/tasks260-plugin-validate-final.log`). Genuine component cases for split response-loss, delayed hydration, pending-file reload, finalize retry, and cancel/new-draft key isolation remain to be expanded; authenticated browser E2E, OCR integration, temporal reflow, and full financial gates remain open.
- Fix-round behavioral matrix: RED `.codex-task-logs/tasks260-ui-red-current.log` reproduced missing evidence error and incorrectly prepared split/cancel fixtures. GREEN focused editor passed `14/14` (`.codex-task-logs/tasks260-ui-matrix-final4.log`), covering stable-key-only unknown membership, pending file reselection, missing prepare URL, failed-finalize retry with the same staging membership, split destination membership, cancel key isolation, delayed hydration after an edit, backend sequence/component rendering without JSON, unsaved reviewed-edit preview blocking, and batch-scoped execute-key replay after remount. Fresh full frontend passed `62 files, 290 tests` (`.codex-task-logs/tasks260-frontend-full-final5.log`); lint/build/backend typecheck passed (`.codex-task-logs/tasks260-frontend-lint-final4.log`, `.codex-task-logs/tasks260-frontend-build-final5.log`, `.codex-task-logs/tasks260-backend-typecheck-final2.log`). Plugin remains synchronized at 9.4.0, 11 skills, 126 tools: `56/56, 1710 expectations` and validator passed (`.codex-task-logs/tasks260-plugin-final.log`, `.codex-task-logs/tasks260-plugin-validator-final.log`). Authenticated browser E2E remains unverified; OCR integration, temporal reflow, and full financial verification remain open.
- Split response-loss RED `.codex-task-logs/tasks261-split-response-red.log` reproduced distinct idempotency keys across a failed then replayed split. GREEN `.codex-task-logs/tasks261-split-response-green.log` now proves the same persisted split key is sent on retry and the destination response is rendered. Temporal reflow remains unimplemented locally: no reflow proposal/group/entry schema or service exists yet, and current reconciliation still performs its existing replacement flow; no unsupported financial shortcut was introduced.
- Task5 prerequisite review: `rg -n "reflow|TemporalReflow|reconciliationReflow" backend/src backend/drizzle` found no local temporal-reflow persistence or service. The current reconciliation path still updates loan balances and creates replacement transactions, so it cannot safely provide append-only allocation reversal/replay without the approved additive schema and authoritative replay kernel. A daily floating `advanceInterestPeriods: 1` health fixture was run through the serialized disposable suite and did not reproduce a duplicate due amount (`13/13`, 36 expectations, `.codex-task-logs/tasks261-health-red.log`); no speculative projection change was made. Post-split frontend gates passed `62 files, 291 tests` (`.codex-task-logs/tasks261-frontend-full-final.log`), build/lint passed (`.codex-task-logs/tasks261-frontend-build-final.log`, `.codex-task-logs/tasks261-frontend-lint-final.log`).
- Task5 status correction for the next checkpoint: the earlier “unimplemented locally” note records observed state at that prior HEAD, not a blocker. The approved temporal-reflow schema/kernel is authorized implementation work. This checkpoint adds the 0070 additive provenance boundary and task-local delta design; authoritative kernel and reconciliation binding remain open and are not claimed complete.
- Task5 foundation checkpoint: RED migration proof is `.codex-task-logs/tasks5-reflow-migration-red.log` (missing 0070 file); GREEN kernel proof is `.codex-task-logs/tasks5-reflow-kernel-green2.log` (`4 passed, 0 failed, 8 expectations`). The serialized disposable migrator applied the real 0069-prefix to full migration upgrade, verified 0070 journal/hash/idempotent rerun plus 3 tables, tenant constraints, and immutable triggers: `.codex-task-logs/tasks5-foundation-final.log` (`7 passed, 0 failed, 24 expectations`); backend typecheck is `.codex-task-logs/tasks5-foundation-typecheck.log`. Added migration 0070 and the task-local delta design. Reconciliation preview/execute binding, append-only DB replay execution, cache rebuild, MCP contract, and daily health work remain open; no financial records were written.
- Task5 fix round: RED `.codex-task-logs/tasks5-reflow-trigger-red2.log` reproduced 0070 accepting proposal identity mutation during lifecycle transition; additive 0071 now compares immutable identity/content fields and enforces executor metadata. RED `.codex-task-logs/tasks5-split-lineage-red.log` covered one-to-many source lineage before 0072; migration 0072 replaces source-only uniqueness with source+replacement uniqueness. Kernel RED `.codex-task-logs/tasks5-kernel-hardening-red.log` covered chronological UUID/date ordering and duplicate/inconsistent provenance; GREEN `.codex-task-logs/tasks5-authoritative-resolver-green.log` passes authoritative-resolver adapter and exact validations. Final serialized migration/kernel gate `.codex-task-logs/tasks5-immutability-kernel-focused.log` was `10 passed, 0 failed, 35 expectations`; subsequent one-to-many and resolver tests are in `.codex-task-logs/tasks5-authoritative-resolver-green.log`. Reconciliation preview/execute binding, append/replay writes, cache rebuild, and full financial integration remain open.
- Task5 hardening final: serialized migration/kernel gate `.codex-task-logs/tasks5-foundation-final3.log` passed `12, 0 failed, 39 expectations`; backend typecheck `.codex-task-logs/tasks5-typecheck-final2.log` and diff-check `.codex-task-logs/tasks5-diffcheck-final2.log` passed. 0071 rejects combined lifecycle identity/tenant/creator/timestamp/snapshot mutation, and 0072 permits conserved one-source-to-many-replacement lineage. The planning API now preserves loan/date chronology, rejects duplicate/inconsistent provenance, and requires the authoritative allocator adapter. Financial append/replay integration, cache rebuild, and reconciliation preview/execute binding remain open.
- Task5 execution checkpoint: resolver validation now completes before any stateful callback and callbacks run in deterministic loan/date/transaction order (`.codex-task-logs/task5-reflow-kernel-final.log`, 10 passed, 0 failed, 17 expectations). Historical explicit interest-only reconciliation now stores the temporal plan in the revision/hash-bound preview, invokes the existing floating allocator, and executes atomically with append-only reversal and replacement transactions plus signed allocation lineage and accrual-cache recomputation; original later transaction/allocation rows remain unchanged. Real disposable service gate `.codex-task-logs/task5-reflow-integration-final.log`: 19 passed, 0 failed, 104 expectations; migration gate `payment-reconciliation-reflow-migration.test.ts`: 4 passed, 0 failed, 25 expectations; backend typecheck `.codex-task-logs/task5-reflow-typecheck-final2.log` passed; `git diff --check` `.codex-task-logs/task5-reflow-diffcheck-final.log` passed. Exact same-key concurrent retry is covered in the integration case. Unsupported principal/fee/penalty and incomplete provenance remain fail-closed. Reflow proposal table remains reserved for repair-origin proposals because migration 0070 explicitly forbids linking automatic groups to proposal rows; the normal reconciliation proposal is the preview authority. OCR/browser, other financial writers, MCP/UI, and full-plan gates remain open.
