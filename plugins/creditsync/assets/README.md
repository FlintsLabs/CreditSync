# CreditSync plugin assets

Version 5.0.0 intentionally omits optional icon, logo, screenshot, and UI fields. Add branded visual assets only after the repository's approved application artwork is available; never point the manifest at a placeholder image.
